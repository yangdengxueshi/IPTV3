<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:background="@color/black"
             tools:context=".activity.live.LivePlayerActivity">

    <com.dexin.wanchuan.iptv3.widget.live.MyMediaPlayer
        android:id="@+id/my_mediaplayer_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </com.dexin.wanchuan.iptv3.widget.live.MyMediaPlayer>

    <LinearLayout
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ListView
            android:id="@+id/programs_list_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#80000000"
            android:divider="#00000000"
            android:dividerHeight="30dp"
            android:listSelector="@mipmap/live_program_selector"
            android:nextFocusRight="@id/programs_list_view"
            android:scrollbars="none"
            android:visibility="gone">
        </ListView>
    </LinearLayout>

    <ImageView
        android:id="@+id/water_mark_image_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:layout_margin="24dp"
        android:maxWidth="200dp"
        android:maxHeight="200dp"
        android:visibility="gone"
        tools:ignore="ContentDescription"/>

    <TextView
        android:id="@+id/program_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right"
        android:layout_marginTop="50dp"
        android:layout_marginRight="70dp"
        android:textColor="#efff99"
        android:textSize="70sp"
        android:textStyle="bold"/>

</FrameLayout>